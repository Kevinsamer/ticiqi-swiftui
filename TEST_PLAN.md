# Ticiqi iPad Professional Teleprompter — 功能测试计划

## 项目概述

iPad 专业提词器应用，支持 GPU 加速渲染、局域网远程控制、Web 实时改稿、Bonjour 自动发现等功能。技术栈：SwiftUI + UIKit 混合架构、FlyingFox、CoreText、CADisplayLink。

---

## 一、渲染引擎 (Core/Engine + Core/Renderer)

### 1.1 DisplayLink 滚动驱动

| 编号 | 测试项 | 测试步骤 | 预期结果 |
|------|--------|---------|---------|
| R-001 | 60Hz 基础滚动 | 加载文稿 → 播放 → 观察滚动 | 文稿以 60 FPS 平滑向上滚动 |
| R-002 | 120Hz ProMotion | 在 ProMotion 设备上播放 → Instruments 检测帧率 | 帧率达到 120 FPS |
| R-003 | deltaTime 匀速性 | 设置速度 30 → 播放 10 秒 → 测量偏移量 | 偏移量 ≈ 300px，无累计误差 |
| R-004 | 播放/暂停切换 | 播放 → 暂停 → 播放 | 无抖动、无跳帧，暂停时位置不变 |
| R-005 | DisplayLink 内存释放 | 播放 → 暂停 → 退出 → Instruments 检查 | 无 CADisplayLink 残留、无循环引用 |

### 1.2 CoreText 渲染 & 瓦片层

| 编号 | 测试项 | 测试步骤 | 预期结果 |
|------|--------|---------|---------|
| R-006 | 1 万字文稿渲染 | 导入 1 万字 .txt → 播放 | 渲染完成，满帧滚动 |
| R-007 | 5 万字文稿渲染 | 导入 5 万字 .txt → 播放 | 满帧，无明显掉帧 |
| R-008 | 10 万字文稿渲染 | 导入 10 万字 .txt → 从头播到尾 | 满帧，0 掉帧，无 OOM |
| R-009 | CATiledLayer 瓦片异步渲染 | 加载长文稿 → 快速滚动 | 瓦片异步加载，无主线程阻塞 |
| R-010 | 字号变化后重排 | 播放中 → 字号从 48 改为 80 | 文字立即变大，布局重排正确 |
| R-011 | 行间距一致性 | 渲染后目视检查 | 行间距 = fontSize × 0.3，均匀一致 |

### 1.3 镜像模式

| 编号 | 测试项 | 测试步骤 | 预期结果 |
|------|--------|---------|---------|
| R-012 | 镜像开关 | 启用镜像 → 观察 | 水平翻转 180°，适配反射玻璃 |
| R-013 | 镜像 + 滚动 | 镜像模式下播放 | 滚动方向不变，文字仍水平镜像 |
| R-014 | 远程切换镜像 | Web 控制台点击镜像按钮 | iPad 立即切换镜像状态 |

### 1.4 焦点线

| 编号 | 测试项 | 测试步骤 | 预期结果 |
|------|--------|---------|---------|
| R-015 | 焦点线显示 | 启用焦点线 | 屏幕中央水平白色半透明线 |
| R-016 | 焦点线隐藏 | 关闭焦点线 | 线条消失 |
| R-017 | 焦点线 + 镜像 | 同时启用 | 焦点线正常显示在中央 |

### 1.5 滚动方向

| 编号 | 测试项 | 测试步骤 | 预期结果 |
|------|--------|---------|---------|
| R-018 | 向上滚动 (默认) | 播放 | 文稿从下向上移动 |
| R-019 | 向下滚动 | 切换方向后播放 | 文稿从上向下移动 |
| R-020 | 远程切换方向 | Web 控制台切换 scrollDirection | iPad 立即切换方向 |

### 1.6 亚像素对齐

| 编号 | 测试项 | 测试步骤 | 预期结果 |
|------|--------|---------|---------|
| R-021 | Retina 屏幕对齐 | 在 Retina iPad 上播放 | 文字边缘无锯齿抖动 |
| R-022 | offset 取整验证 | 检查 syncOffsetToView 计算 | offset 经 scale 取整对齐 |

---

## 二、文稿系统 (Core/Script + Core/Model)

### 2.1 文件导入

| 编号 | 测试项 | 测试步骤 | 预期结果 |
|------|--------|---------|---------|
| S-001 | 导入 .txt 文件 | 文件选择器 → 选 .txt | 成功导入，标题 = 文件名 |
| S-002 | 导入 .md 文件 | 文件选择器 → 选 .md | 成功导入，标题识别正确 |
| S-003 | 导入空文件 | 导入空 .txt | 抛出 emptyFile 错误，不崩溃 |
| S-004 | 导入无权限文件 | 模拟 accessDenied | 抛出 accessDenied 错误 |
| S-005 | 导入读取失败 | 模拟文件损坏 | 抛出 readFailed 错误 |
| S-006 | 行尾符兼容 | 导入含 \r\n 的文件 | 自动标准化为 \n |
| S-007 | 后台 I/O 线程 | 导入大文件 → 检查线程 | I/O 在 background 线程执行 |

### 2.2 Web 上传

| 编号 | 测试项 | 测试步骤 | 预期结果 |
|------|--------|---------|---------|
| S-008 | Web 上传 .txt | Web 控制台 → 选择 .txt → 上传 | iPad 立即加载文稿 |
| S-009 | Web 上传 .docx | Web 控制台 → 选择 .docx → 上传 | 解析纯文本，iPad 加载 |
| S-010 | Web 上传空内容 | 上传空字符串 | 服务端返回 400，不崩溃 |
| S-011 | Web 上传大文件 | 上传 10 万字文稿 | 成功接收并加载 |

### 2.3 段落解析

| 编号 | 测试项 | 测试步骤 | 预期结果 |
|------|--------|---------|---------|
| S-012 | 普通文稿段落解析 | 导入纯文本（无 # 标题） | 所有段落 markerType = .normal |
| S-013 | Markdown 标题识别 | 导入含 # / ## / ### 的 .md | 标题段落 markerType = .heading |
| S-014 | characterOffset 预计算 | 检查 Paragraph.characterOffset | 每段偏移 = 前面所有段落字符数之和 + 换行符 |
| S-015 | 空行跳过 | 文稿含连续空行 | 空行不生成 Paragraph |

### 2.4 段落索引 & 导航

| 编号 | 测试项 | 测试步骤 | 预期结果 |
|------|--------|---------|---------|
| S-016 | 段落索引 O(P) | 加载文稿 → rebuildIndex | 返回 paragraphPositions 数组 |
| S-017 | 标题过滤 | headingPositions | 仅返回 .heading 类型段落 |
| S-018 | 标题列表点击跳转 | 打开段落导航 → 点击标题 | 滚动到对应进度位置 |
| S-019 | 进度百分比显示 | 检查 ParagraphPosition.estimatedProgress | 0.0 ~ 1.0，字符数比例 |
| S-020 | 空文稿导航 | 无文稿时打开段落导航 | 显示 ContentUnavailableView |
| S-021 | 无标题文稿导航 | 纯文本打开段落导航 | 显示"无标题段落"提示 |

### 2.5 热更新

| 编号 | 测试项 | 测试步骤 | 预期结果 |
|------|--------|---------|---------|
| S-022 | Web 热更新文稿 | 播放中 → Web 上传新文稿 | 内容更新，滚动继续不中断 |
| S-023 | 进度保持 | 播放到 50% → 热更新 | 更新后仍在约 50% 位置 |
| S-024 | 首次加载 | 无文稿 → Web 上传 | 创建新文稿，从头开始 |
| S-025 | 标题更新 | 热更新时传入新 title | 标题正确更新 |

### 2.6 文稿存储

| 编号 | 测试项 | 测试步骤 | 预期结果 |
|------|--------|---------|---------|
| S-026 | 保存文稿 | 导入文稿 → 检查 scripts.json | 文件写入 Documents/scripts.json |
| S-027 | 加载文稿列表 | 启动 app | 按 updatedAt 降序显示 |
| S-028 | 重命名文稿 | 长按卡片 → 重命名 → 确认 | 标题更新，排序不变 |
| S-029 | 删除文稿 | 长按卡片 → 删除 → 确认 | 文稿从列表和 JSON 中移除 |
| S-030 | 重启后持久化 | 导入文稿 → 杀掉 app → 重启 | 文稿列表完整恢复 |

---

## 三、局域网控制系统 (Network)

### 3.1 HTTP 服务

| 编号 | 测试项 | 测试步骤 | 预期结果 |
|------|--------|---------|---------|
| N-001 | 服务启动 | 自动启动服务器 | 端口 8080 监听成功 |
| N-002 | IPv4 绑定 | 浏览器访问 http://iPad-IP:8080 | 可正常访问 |
| N-003 | 静态文件 - HTML | GET / | 返回 index.html (text/html) |
| N-004 | 静态文件 - JS | GET /app.js | 返回 app.js (application/javascript) |
| N-005 | 静态文件 - CSS | GET /style.css | 返回 style.css (text/css) |
| N-006 | 文件上传路由 | POST /upload | 返回 200 OK |
| N-007 | 空上传拒绝 | POST /upload 空 body | 返回 400 |
| N-008 | 服务关闭 | 手动停止服务器 | 端口释放，浏览器无法连接 |

### 3.2 WebSocket 连接

| 编号 | 测试项 | 测试步骤 | 预期结果 |
|------|--------|---------|---------|
| N-009 | WebSocket 握手 | 浏览器打开控制台 | 自动连接 ws://iPad-IP:8080/ws |
| N-010 | 心跳保活 | 连接后等待 30 秒 | 连接不中断 |
| N-011 | 客户端 ping | 客户端每 5 秒发送 ping | 服务端刷新 lastSeenAt |
| N-012 | 死连检测 | 客户端停止 ping 超 8 秒 | 客户端主动断开重连 |
| N-013 | 服务端超时清理 | 客户端 10 秒无消息 | 服务端 unregister，clientCount 更新 |
| N-014 | 页面刷新去重 | 刷新浏览器页面 | 旧连接清理，clientCount 不虚高 |
| N-015 | 多客户端连接 | 同时打开 3 个浏览器标签 | clientCount = 3 |

### 3.3 控制命令

| 编号 | 测试项 | 测试步骤 | 预期结果 |
|------|--------|---------|---------|
| N-016 | 远程播放 | Web 点击播放 | iPad 开始滚动 |
| N-017 | 远程暂停 | Web 点击暂停 | iPad 停止滚动 |
| N-018 | 远程调速 + | Web 点击速度 + | 速度增加 5，范围 1-200 |
| N-019 | 远程调速 - | Web 点击速度 - | 速度减少 5 |
| N-020 | 远程字号 + | Web 点击字号 + | 字号增加 2，范围 20-120 |
| N-021 | 远程字号 - | Web 点击字号 - | 字号减少 2 |
| N-022 | 远程 seek | Web 拖拽进度条到 75% | iPad 滚动到 75% 位置 |
| N-023 | 远程镜像切换 | Web 点击镜像 chip | iPad 切换镜像状态 |
| N-024 | 远程焦点线切换 | Web 点击焦点线 chip | iPad 切换焦点线 |
| N-025 | 远程方向切换 | Web 切换 scrollDirection | iPad 切换滚动方向 |
| N-026 | 远程文稿更新 | Web 上传新文稿 | iPad 热更新内容 |

### 3.4 状态广播

| 编号 | 测试项 | 测试步骤 | 预期结果 |
|------|--------|---------|---------|
| N-027 | 周期性广播 | 播放 → 监听 WebSocket 消息 | 每 30 帧收到 StateSnapshot |
| N-028 | 即时广播 | 本地操作后 | 立即收到状态更新 |
| N-029 | contentDirty 节流 | 无文稿变更时 | StateSnapshot.content 为空字符串 |
| N-030 | content 发送 | 文稿变更后首次广播 | StateSnapshot.content 包含全文 |
| N-031 | 进度条同步 | iPad 播放 → Web 进度条 | Web 进度条实时跟随 |
| N-032 | 播放状态同步 | iPad 暂停 → Web 按钮 | Web 按钮切换为播放图标 |

### 3.5 多客户端同步

| 编号 | 测试项 | 测试步骤 | 预期结果 |
|------|--------|---------|---------|
| N-033 | 命令广播 | 客户端 A 发送 play | 客户端 B、C 也收到状态更新 |
| N-034 | 客户端断开通知 | 客户端 A 断开 | 其余客户端 clientCount 更新 |
| N-035 | 僵尸客户端清理 | 客户端静默断开 | 10 秒后自动清理 |

### 3.6 网络权限

| 编号 | 测试项 | 测试步骤 | 预期结果 |
|------|--------|---------|---------|
| N-036 | 首次权限弹窗 | 首次启动 app | 弹出本地网络权限请求 |
| N-037 | 权限拒绝后降级 | 拒绝权限 | 服务无法启动，UI 显示提示 |
| N-038 | Bonjour 广播 | 启动服务 | NetService 发布 _http._tcp |

---

## 四、Bonjour 自动发现 (Milestone 4)

| 编号 | 测试项 | 测试步骤 | 预期结果 |
|------|--------|---------|---------|
| B-001 | mDNS 发布 | 启动服务 | MyTeleprompter.local 可被发现 |
| B-002 | 域名访问 | 浏览器输入 MyTeleprompter.local:8080 | 正常加载控制台 |
| B-003 | 同局域网发现 | 另一台设备访问 | 无需输入 IP 即可连接 |

---

## 五、外设控制 (Milestone 5)

| 编号 | 测试项 | 测试步骤 | 预期结果 |
|------|--------|---------|---------|
| E-001 | Space 播放/暂停 | 外接键盘按 Space | 切换播放/暂停 |
| E-002 | ArrowUp 调速 | 按 ArrowUp | 速度增加 |
| E-003 | ArrowDown 调速 | 按 ArrowDown | 速度减少 |
| E-004 | PageUp 段落跳转 | 按 PageUp | 跳转到上一个段落 |
| E-005 | PageDown 段落跳转 | 按 PageDown | 跳转到下一个段落 |
| E-006 | Volume+ 翻页器 | 蓝牙翻页器按 Volume+ | 速度增加 |
| E-007 | Volume- 翻页器 | 蓝牙翻页器按 Volume- | 速度减少 |
| E-008 | 自定义映射 | 配置 KeyMapping JSON | 按键按映射执行 |

---

## 六、生命周期与稳定性 (Milestone 6)

### 6.1 前后台切换

| 编号 | 测试项 | 测试步骤 | 预期结果 |
|------|--------|---------|---------|
| L-001 | 进入后台暂停 | 播放中按 Home 键 | DisplayLink 暂停，播放停止 |
| L-002 | 恢前台恢复 | 返回 app | 如果之前在播放则自动恢复 |
| L-003 | 多次切换 | 反复切前台/后台 10 次 | 无崩溃、无状态错乱 |
| L-004 | 屏幕常亮 | app 活跃时 | isIdleTimerDisabled = true，不锁屏 |
| L-005 | 后台允许锁屏 | app 进入后台 | isIdleTimerDisabled = false |

### 6.2 网络异常

| 编号 | 测试项 | 测试步骤 | 预期结果 |
|------|--------|---------|---------|
| L-006 | WiFi 断开 | 关闭 WiFi | HTTP/WS 服务自动关闭 |
| L-007 | WiFi 恢复 | 重新连接 WiFi | 服务自动重启 |
| L-008 | 客户端断连 | 浏览器断开网络 | 服务端 10 秒后清理连接 |
| L-009 | 客户端重连 | 浏览器恢复网络 | 自动重连 WebSocket |

### 6.3 内存安全

| 编号 | 测试项 | 测试步骤 | 预期结果 |
|------|--------|---------|---------|
| L-010 | CADisplayLink 循环引用 | Instruments Leaks 检查 | 无循环引用（Proxy 弱引用机制） |
| L-011 | WebSocket 内存释放 | 连接 → 断开 → 重复 10 次 | 内存无持续增长 |
| L-012 | 大文稿内存 | 加载 10 万字 → 播放 → 退出 | 内存正常释放 |
| L-013 | 长时间运行 | 播放 30 分钟 | 无内存泄漏、无崩溃 |

---

## 七、设置系统

| 编号 | 测试项 | 测试步骤 | 预期结果 |
|------|--------|---------|---------|
| C-001 | 默认速度设置 | 设置页修改默认速度 → 进入提词器 | 提词器使用新默认速度 |
| C-002 | 字号设置 | 修改字号 → 进入提词器 | 字号生效 |
| C-003 | 滚动方向设置 | 切换方向 → 进入提词器 | 方向生效 |
| C-004 | 文字颜色 | 选择 green → 进入提词器 | 文字为绿色 |
| C-005 | 背景色 | 选择 darkGray → 进入提词器 | 背景为深灰 |
| C-006 | 镜像默认值 | 开启镜像 → 进入提词器 | 镜像默认开启 |
| C-007 | 焦点线默认值 | 关闭焦点线 → 进入提词器 | 焦点线默认关闭 |
| C-008 | 自动启动服务 | 关闭自动启动 → 重启 app | 服务不自动启动 |
| C-009 | 设置持久化 | 修改设置 → 杀掉 app → 重启 | 设置完整恢复 |
| C-010 | 防抖保存 | 快速连续修改设置 | 500ms 防抖后保存一次 |

---

## 八、UI 交互

### 8.1 稿件库

| 编号 | 测试项 | 测试步骤 | 预期结果 |
|------|--------|---------|---------|
| U-001 | 卡片网格显示 | 有 5+ 文稿 | 自适应网格布局，卡片信息完整 |
| U-002 | 新建卡片 | 查看"导入新文稿"卡片 | 虚线边框 + 图标，可点击 |
| U-003 | 卡片点击进入 | 点击文稿卡片 | 跳转到提词器视图 |
| U-004 | 长按菜单 | 长按卡片 | 弹出重命名/删除菜单 |
| U-005 | 服务器地址显示 | 底角显示 badge | IP 地址 + Bonjour 域名 |
| U-006 | 设置入口 | 点击齿轮图标 | 弹出设置 Sheet |

### 8.2 提词器视图

| 编号 | 测试项 | 测试步骤 | 预期结果 |
|------|--------|---------|---------|
| U-007 | 返回按钮 | 点击左上角返回 | 暂停播放 → 返回稿件库 |
| U-008 | 状态栏隐藏 | 进入提词器 | 状态栏和导航栏隐藏 |
| U-009 | 控制面板 | 查看底部控制栏 | 播放按钮 + 进度条 + 展开按钮 |
| U-010 | 进度条拖拽 | 拖拽 Slider | 拖拽时暂停，松手后恢复 |
| U-011 | 速度预设按钮 | 展开面板 → 点击"慢/中/快/极速" | 速度切换为 15/30/60/100 |

### 8.3 Web 控制台

| 编号 | 测试项 | 测试步骤 | 预期结果 |
|------|--------|---------|---------|
| U-012 | Material Design 3 | 打开控制台 | 深色主题、圆角卡片、胶囊按钮 |
| U-013 | 连接状态指示 | 连接/断开 | 绿点/红点切换 |
| U-014 | 设备在线数 | 多客户端连接 | 显示正确数量 |
| U-015 | 播放按钮联动 | 点击播放/暂停 | 图标 ▶ ↔ ⏸ 切换，文字同步 |
| U-016 | 文件上传按钮 | 选择 .txt/.docx | 上传成功，iPad 加载文稿 |

---

## 九、边界条件 & 压力测试

| 编号 | 测试项 | 测试步骤 | 预期结果 |
|------|--------|---------|---------|
| X-001 | 空文稿操作 | 无文稿时尝试播放 | 不崩溃，无操作 |
| X-002 | 超长单行 | 导入含 1 万字符单行的文稿 | 正常渲染和滚动 |
| X-003 | 特殊字符 | 导入含 emoji、CJK、RTL 字符的文稿 | 正常显示 |
| X-004 | 并发操作 | 快速连续点击播放/暂停 | 无状态错乱 |
| X-005 | 同时本地+远程操作 | 本地 Slider + Web seek 同时 | 最后操作生效，无冲突 |
| X-006 | 快速 seek | 连续快速拖拽进度条 | 无卡顿，最终位置正确 |
| X-007 | 极端速度 | 速度 = 1 和速度 = 200 | 均可正常滚动 |
| X-008 | 极端字号 | 字号 = 16 和字号 = 160 | 均可正常渲染 |
| X-009 | 网络断开时操作 | WiFi 断开后继续使用本地功能 | 本地播放/暂停/seek 正常 |
| X-010 | 旋转屏幕 | 横竖屏切换 | 布局自适应，无裁切 |

---

## 测试统计

| 模块 | 测试用例数 |
|------|-----------|
| 渲染引擎 | 22 |
| 文稿系统 | 30 |
| 局域网控制 | 38 |
| Bonjour 自动发现 | 3 |
| 外设控制 | 8 |
| 生命周期与稳定性 | 13 |
| 设置系统 | 10 |
| UI 交互 | 16 |
| 边界条件 & 压力测试 | 10 |
| **总计** | **150** |

---

## 备注

当前项目无自动化测试（无 XCTest target），建议优先对以下纯逻辑层编写单元测试：

- `ScriptDocument` — 段落解析、行尾标准化、updateContent
- `ParagraphIndexer` — 索引构建、标题过滤、进度估算
- `CommandParser` — JSON 命令解析、边界值
- `CoreTextMetrics` — totalHeight 计算
- `ScriptImporter` — 格式检测、错误处理

UI 和网络层以手动/集成测试为主。
