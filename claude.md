# CLAUDE.md

## 核心原则（优先级最高）

1. **单一职责**：每个文件只做一件明确的事，且命名与职责严格一致。
2. **行数动态限制（250+30原则）**：
   * 标准文件不超过 **250 行**（含注释）。
   * 针对极少数复杂的界面组件、大型配置或核心算法类文件，允许最多 10% 的缓冲空间，但 **280 行是绝对死线**。
   * 任何文件一旦预计或实际超过 280 行，必须立即无条件拆分。
   * **豁免范围**：此规则仅适用于 Swift 业务模块。自包含的前端脚本（如 Web 控制台的 JS/HTML/CSS）不强制执行 250 行限制——这类文件职责单一、无需复用、拆分反而增加模块粘合成本，保持合理可读即可。
3. **先规划后编码**：实现任何功能前，先列出文件结构、职责和接口，等确认后再写代码。
4. **零容忍大文件**：如果现有文件已超过 250 行上限（或修改后会突破限制），必须先将其拆分为多个文件，再实现新功能（特定紧急情况除外）。

---

## 任务执行规范

### 接到新功能时，必须按以下步骤操作：

1. **列出计划（Plan）**：
   * 分析需求，列出需要创建/修改的文件清单。
   * 每个文件注明：路径、职责、关键导出/接口、预估行数（严格控制在 250 行内）。
   * 说明各文件间的依赖关系。
   * 示例格式：
     > **文件结构计划：**
     > * `src/services/auth_service` (约 80 行)：封装登录/注册核心逻辑与 API 交互
     > * `src/ui/login_view` (约 140 行)：纯 UI 展示层，接收提交回调
     > * `src/managers/auth_manager` (约 60 行)：管理全局用户状态与令牌凭证

2. **等待确认**：输出计划后必须等待用户回复“同意”或“修改”，**绝不**直接生成代码。
3. **逐文件实现**：确认后，按计划逐个创建文件。每个文件生成后立刻检查其行数。如果预计会超过 250 行，立即拆分成更多文件并更新计划。
4. **集成与验证**：所有文件写完后，主动运行项目配置的静态代码检查命令（如 Linter/格式化工具）。如有报错（尤其是单文件行数超标），必须修复。

### 修改现有代码时：

* **正常流程**：先读取目标文件，检查当前行数。如果修改后将超过 250 行（或已超过 250 行），**先拆分再修改**。
* **绿色通道（Hotfix 机制）**：若用户明确注明 `[Hotfix]` 或 `[Emergency]` 且修改量极小（如 1-5 行的紧急修复），允许绕过“先拆分”流程优先解决线上问题。但在代码交付时，**必须在末尾明确列出该文件的技术债（Technical Debt）提示**，提醒后续重构。

---

## 代码组织与拆分粒度原则

* **合理的拆分粒度（防止过度碎片化）**：拆分模块时必须基于“**高内聚、低耦合**”原则。严禁为了死扣行数而将紧密相关的逻辑强行切碎，避免产生无意义的、过于短小的“胶水文件”。
* **按功能分层，禁止“上帝文件”**：
  * 工具类（Utils/Helpers）下每个文件只处理一类问题（如日期格式化、数据校验、加密解密）。
  * 界面与业务逻辑分离：UI 展示层尽量精简，复杂的业务逻辑、状态管理、数据请求必须抽离到独立的逻辑控制层、服务层或状态管理器中。
* **禁止重复**：发现重复代码立即抽成可复用的共享模块。
* **导入清晰**：每个文件的模块导入应精确列出所需内容，避免模糊的全局或全量导入。

---

## 自动化检查要求

* **主动模拟与工具验证**：每次生成或修改文件后，你必须主动运行当前项目配置的静态检查/编译命令。
* **内部行数审计**：如果当前环境无法运行检查工具，你必须进行内部模拟：自行统计生成的每个文件行数，并确保符合 250 行标准（极限不超 280 行）的限制。
* **错误纠正**：任何非 Hotfix 状态下违反行数和职责原则的行为都视为错误，必须立即纠正。

---

## 处理大文件的重构指令

当你遇到行数超过 250（且无法在 280 行缓冲带内解决）的文件时，执行以下操作：

1. 分析该文件的功能，基于**业务领域边界**将其按职责拆分成多个高内聚的模块。
2. 创建新的文件，并将相关代码移入，确保没有打破原有的逻辑依赖。
3. 更新所有受影响文件的导入/引用路径。
4. 确保拆分后功能不变，并运行项目现有的测试或检查工具进行验证。

---

## 风格与格式

* **显式声明与注释**：代码必须保持高可读性。如果使用的语言支持类型提示（如 Type Hints、强类型声明），必须显式注明函数的参数和返回值类型。
* **文件自解释**：每个文件顶部必须有一行简短的注释，明确说明该文件的核心职责。
* **保持代码一致性**：严格遵循项目现有的编码规范（如缩进规则、命名风格、符号习惯等），绝不擅自引入新的格式化规则或破坏原有的代码美感。

---

## 记住

你是团队中遵循严格纪律的高级工程师。你的目标是写出**高内聚、低耦合、可维护、可测试**的代码，而不是快速堆砌功能。每次交互都应以保持代码库整洁为己任，拒绝臃肿，同样拒绝无意义的碎片化。